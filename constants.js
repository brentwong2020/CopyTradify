const DIRECTION_MAP = {
    CN_TO_HK: 'cn2hk',
    HK_TO_CN: 'hk2cn',
};

const LOCALE = {
    HK: 'hk',
    CN: "cn",
}

const STORAGE_KEY = {
    ENABLED: 'enabled',
    DIRECTION: 'direction',
}

const ALL_DIRECTIONS = Object.values(DIRECTION_MAP);
const ALL_LOCALE = Object.values(DIRECTION_MAP);